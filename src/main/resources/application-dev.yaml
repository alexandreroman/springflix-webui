logging:
  level:
    com.vmware.tanzu: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"
  tracing:
    enabled: false
  otlp:
    metrics:
      export:
        enabled: false
